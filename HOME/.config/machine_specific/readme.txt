All machine-specific configs should go here.

Their filenames should match the parent configs, such as
the machine-specific .gitconfig being named .gitconfig,
instead of .gitconfig_machine_specific as it was previously.
