#!/usr/bin/env bash
# {x}, creating directories if necessary
# ${@: -1} is a bash/zsh-ism for the last arg
d="$(dirnameslash "${@: -1}")"
if [[ ! -d "$d" ]]; then
  echo "Creating '$d'"
  mkdir -p -- "$d"
fi
"$@"
